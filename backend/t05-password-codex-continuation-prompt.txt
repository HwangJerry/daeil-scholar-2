Use this as the exact Codex continuation prompt.

Start with:
1) Workdir: /Users/jerryhwang/Workspace/03_daeil/.worktrees/dflh-saf-v2-mvp/web/backend
2) Branch context: feature/mvp-implementation
3) Constraints:
   - Do NOT run: git reset, git checkout, git clean.
   - Keep MysqlNativePassword legacy fallback (do not delete legacy compatibility).
   - Do NOT output real secrets/tokens/passwords.
   - Canonical hashes are immutable:
     CANONICAL_CANDIDATE_MANIFEST_SHA256 = d880ed4b4d4b6b7dde2ef928490799979f37f3e9bb98b56bbbd9d5b6e0100d90
     CANONICAL_SCHEMA_CONTRACT_MANIFEST_SHA256 = e708e177dadc8d6f84b894c74385a2a2114e954f3e3fe900b459347981195e6c
   - Follow TDD: RED tests then implementation.

Load skill first:
- software-development/t05-password-continuation
- If skill auto-load fails, follow this message directly using the same sequence.

Active task state to continue:
- [>] t05-password. implement Argon2id/legacy verification, rehash, and global semaphore with tests (in_progress)
- [>] t05-password-tests. Add RED tests for password hasher behavior and service transitions (in_progress)
- [ ] t05-password-service. Migrate member/registration/password-change/reset service paths from MysqlNativePassword to credential-based verifier (pending)
- [ ] t05-password-regression. Run focused + full backend regression for changed packages (pending)

Startup hooks (run in order):
1. `git status`
2. `git branch --show-current`
3. `read_file internal/service/password_service.go`
4. `read_file internal/service/password_service_test.go`
5. `read_file internal/service/member_service.go`
6. `read_file internal/service/registration_service.go`
7. `read_file internal/service/password_change_service.go`
8. `read_file internal/service/password_reset_service.go`
9. `read_file internal/repository/interfaces.go`
10. `read_file go.mod`

Execution plan:
1) Finish RED tests for password hasher + transition behavior in internal/service/password_service_test.go.
2) Implement/extend `internal/service/password_service.go` with:
   - Argon2id verification
   - MysqlNativePassword fallback
   - deterministic rehash condition
   - global in-process semaphore around expensive hash verification/update to avoid thundering herd
3) Migrate service paths to credential-first flow with legacy fallback:
   - internal/service/member_service.go
   - internal/service/registration_service.go
   - internal/service/password_change_service.go
   - internal/service/password_reset_service.go
4) Update/extend tests for service transition coverage.
5) Run regression hooks in order:
   - `go test ./internal/repository/...`
   - `go test ./internal/service/...`
   - `go test ./internal/handler/...`
   - `go test ./...`

Blocker handling hook:
- On failures, record: blocker, exact failing command, test symbol, root-cause.
- Fix only scoped changes and rerun minimum failing scope first before broader scope.

Completion checks:
- Legacy path still works (MysqlNativePassword fallback preserved).
- Credential auth path used first when available.
- Rehash path tested + deterministic.
- t05-password-service starts and reaches in_progress/completed with evidence.
- Full + focused regressions pass.

Machine-readable snippet to keep context:
{
  "handoff": {
    "skill_path": "/Users/jerryhwang/.hermes/skills/software-development/t05-password-continuation/SKILL.md",
    "workdir": "/Users/jerryhwang/Workspace/03_daeil/.worktrees/dflh-saf-v2-mvp/web/backend",
    "branch": "feature/mvp-implementation",
    "must_not_do": ["git reset", "git checkout", "git clean", "print secrets"],
    "constraints": ["MysqlNativePassword fallback must remain", "arg2+legacy-compatible", "no secret leakage"],
    "active_tasks": [
      "t05-password",
      "t05-password-tests",
      "t05-password-service",
      "t05-password-regression"
    ],
    "canonical_hashes": {
      "CANONICAL_CANDIDATE_MANIFEST_SHA256": "d880ed4b4d4b6b7dde2ef928490799979f37f3e9bb98b56bbbd9d5b6e0100d90",
      "CANONICAL_SCHEMA_CONTRACT_MANIFEST_SHA256": "e708e177dadc8d6f84b894c74385a2a2114e954f3e3fe900b459347981195e6c"
    }
  },
  "hooks": {
    "startup": ["git status", "git branch --show-current", "read_file internal/service/password_service.go", "read_file internal/service/password_service_test.go", "read_file internal/service/member_service.go", "read_file internal/service/registration_service.go", "read_file internal/service/password_change_service.go", "read_file internal/service/password_reset_service.go", "read_file internal/repository/interfaces.go", "read_file go.mod"],
    "regression": ["go test ./internal/repository/...", "go test ./internal/service/...", "go test ./internal/handler/...", "go test ./..."]
  }
}

If you hit a hook or tool output gap, do not stop: continue with next deterministic step and report blocker+next command only.

---

Completion update (2026-08-12):

- [x] t05-password. Added algorithm-tagged ARGON2ID and MYSQL_NATIVE_PASSWORD verification, deterministic rehash, bounded Argon2id inputs, and a process-wide hash semaphore with capacity 1.
- [x] t05-password-tests. Added policy, legacy transition, status, CAS rehash, resource-bound, salt-length, and benchmark coverage.
- [x] t05-password-service. Added canonical-first member authentication with fail-closed disabled/revoked states and legacy fallback only when canonical credentials are absent.
- [x] t11-password-signup. Member, AUTH_ACCOUNT_STATE, alumni verification, LOCAL_USERNAME identity, password credential, and signup tags now commit in one transaction.
- [x] t12-password-mutation. Password change and reset now update canonical and legacy password state atomically. Reset tokens are hashed at rest, legacy links remain valid for their bounded rollout lifetime, all outstanding account reset tokens are consumed, and active sessions are revoked.
- [x] t05-password-regression. Repository, service, handler, full backend, race, vet, diff, manifest, pinned MariaDB 10.1.38 reset-concurrency, and canonical migration harness checks passed.
- [x] independent-review. Two independent read-only implementation reviewers returned PASS after findings were fixed and re-reviewed.

Production activation gate:

- Canonical password constructors remain disabled until the required canonical tables exist.
- The latest migration run must be conflict-free, completed, fully journaled, and APPLIED.
- `043_finalize_identity_authority.sql` must exist in `_migration_history`.
- The legacy WEO_MEMBER authority triggers must be absent.

Verification evidence:

- `go test ./internal/repository/...`
- `go test ./internal/service/...`
- `go test ./internal/handler/...`
- `go test ./...`
- `go test -race ./internal/service ./internal/repository -run 'Password|Credential|Identity|Signup|Canonical' -count=1`
- `go vet ./...`
- `PASSWORD_RESET_DOCKER_INTEGRATION=1 go test ./internal/repository -run '^TestPasswordResetRepositorySerializesDistinctTokensOnMariaDB101$' -count=1 -v`
- `scripts/test-canonical-identity-migrations.sh --verify` with the pinned manifest approvals: `HARNESS_VERIFY=PASS`

Next operational steps:

1. Keep migration 043 as a maintenance-window cutover.
2. Deploy the application only after the production activation gate is true.
3. Monitor canonical authentication rejection, reset failure, and database rollback metrics during rollout.
