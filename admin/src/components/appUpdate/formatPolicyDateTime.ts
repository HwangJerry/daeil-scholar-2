// formatPolicyDateTime — app update policy screens' names for the shared Seoul-time helpers
export {
  formatSeoulDateTime as formatPolicyDateTime,
  formatOperatorLabel as formatOperator,
} from '../../lib/formatSeoulDateTime.ts';
